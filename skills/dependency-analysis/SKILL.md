---
name: dependency-analysis
description: Analyze evidence-backed imports, calls, references, inheritance, and dynamic dependencies. Use to answer callers/callees questions, validate code relationships, or propose Context Graph edges without writing the graph.
---

# Dependency Analysis

Call `dependency_analyze_dependencies`, `dependency_find_callers`, `dependency_find_callees`, or `dependency_validate_relationship`. Preserve evidence and confidence. Keep dynamic low-confidence facts separate and never convert every import into Context Graph policy.
